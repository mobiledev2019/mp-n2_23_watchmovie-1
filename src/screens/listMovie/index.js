import React, { Component } from 'react';
import { View, Text } from 'react-native';

class ListMovie extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Danh sách phim đã lưu </Text>
      </View>
    );
  }
}

export default ListMovie;
const styles = StyleSheet.create({
    
});
